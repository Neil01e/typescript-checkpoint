This project demonstrates creating a TypeScript interface Vehicle with properties make, model, year, and a start() method. A Car class implements this interface, initializing its properties through a constructor and overriding the start() method to log "Car engine started". An instance of Car is created to test the functionality, verifying that the start() method works as expected :D
